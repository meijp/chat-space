.chat-group-form
  %h1 チャットグループ編集
  %form#edit_chat_group_22.edit_chat_group{"accept-charset": "UTF-8", action: "/chat_groups/", method: :post}
    .chat-group-form__errors
      - if @group.errors.any?
        %h2
          = @group.errors.count
          件のエラーが発生しました。
        %ul
          -@group.errors.full_messages.each do |message|
            %li
              =message
  = render "groupmember"
